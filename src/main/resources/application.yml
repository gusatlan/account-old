spring:
  sql:
    init:
      platform: postgres

  datasource:
    url: "jdbc:postgresql://${DB_HOST:db}:5432/${DB_NAME:account}"
    username: "${DB_USERNAME:root}"
    password: "${DB_PASSWORD:root}"
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    properties:
      show_sql: true
      format_sql: true